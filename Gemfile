source "https://rubygems.org"

# Use the github-pages meta-gem so our local build matches what
# GitHub Pages serves. This pins Jekyll to a Pages-compatible version.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.7"
